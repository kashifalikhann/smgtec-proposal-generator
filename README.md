# SMGTEC Proposal Generator

Single-page HTML app to generate professional IT service proposals.

## Usage

Open `index.html` in a browser, fill in the sidebar fields, click **Generate**.

## Deploy

Deploy `index.html` to any static host (Netlify, Vercel, GitHub Pages, etc).
